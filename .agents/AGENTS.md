# Behavioral Rules for Segna Ore

- You can and should make automatic `git commit` and `git push` commands. Use clear keywords for the update and a brief description for the commit message (e.g. `feat(sync): description` or `fix(ui): description`).
- Do not perform web searches (`search_web`) or browse the live site on the web using the browser subagent unless explicitly requested by the user.
